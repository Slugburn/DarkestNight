<Window x:Class="Slugburn.DarkestNight.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Slugburn.DarkestNight.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="Darkest Night" Height="480" Width="640" >
    <Window.DataContext>
        <vm:Player />
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="DataTemplates/Blight.xaml"/>
                <ResourceDictionary Source="DataTemplates/Conflict.xaml"/>
                <ResourceDictionary Source="DataTemplates/Hero.xaml"/>
                <ResourceDictionary Source="DataTemplates/Location.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0">
            <TextBlock.Inlines>
                <Run FontWeight="Bold" Text ="Darkness:" />
                <Run FontWeight="Bold" Text ="{Binding Darkness}" />
            </TextBlock.Inlines>
        </TextBlock>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Locations}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Heroes}" >
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <ContentControl Grid.Row="0" Grid.RowSpan="2" Content="{Binding Conflict, diag:PresentationTraceSources.TraceLevel=High}" Visibility="{Binding Conflict.Visibility}"
                        Background="LightGray" Opacity="50">
        </ContentControl>
    </Grid>
</Window>
